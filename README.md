# Stable video diffusion test project
---
## Run instructions  

```bash
  docker build -t stable-video-diffusion-test:latest .
  docker run --gpus all -v /path/to/desired/volume:/app stable-video-diffusion-test:latest
```
